name: Build & Release

on:
  push:
    branches: [main]
    tags: ['v*']                    # case 3: 打 tag → build + release
  pull_request:
    branches: [main]
  workflow_dispatch:                 # case 3: 手动触发构建，不 push 代码

concurrency:
  # push main / push tag / workflow_dispatch 共享同一组
  # 新 push 到来时取消正在进行的 run
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── 1. 过滤：仅针对 push 到 main 分支时检查文件变动 ─────────────────────
  #     tag push 和 workflow_dispatch 直接跳过此 job，让 build 自然运行
  should-build:
    runs-on: ubuntu-latest
    # 仅 main 分支 push 才运行；tag push / workflow_dispatch 跳过整个 job
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    outputs:
      value: ${{ steps.filter.outputs.changed }}
    steps:
      - uses: actions/checkout@v4

      - uses: dorny/paths-filter@v2
        id: filter
        with:
          filters: |
            core: |
              src/**
              src-tauri/**
              package.json
              package-lock.json
              Cargo.toml
              Cargo.lock
              tauri.conf.json

  # ── 2. 构建（三平台）─────────────────────────────────────────────────
  build:
    # 依赖 should-build：当 main push 有代码改动时才运行
    # tag push / workflow_dispatch 无视 should-build 输出，直接运行
    needs: should-build
    if: |
      github.event_name == 'workflow_dispatch'
      || (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v'))
      || (github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.should-build.outputs.value == 'true')
    strategy:
      fail-fast: false
      matrix:
        platform: [ubuntu-22.04, macos-latest, windows-latest]

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install system dependencies (Ubuntu)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libgtk-3-dev

      - name: Install system dependencies (macOS)
        if: matrix.platform == 'macos-latest'
        run: brew install create-dmg

      - name: Install npm dependencies
        run: npm ci

      - name: Build Tauri app
        run: npm run tauri:build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: markflow-${{ matrix.platform }}
          path: |
            src-tauri/target/release/bundle/**
          retention-days: 5

  # ── 3. 创建 Release（仅 tag push 时触发）──────────────────────────────
  release:
    needs: build
    # 仅在 tag push 时执行；workflow_dispatch 手动触发时跳过
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            artifacts/**/*.msi
            artifacts/**/*.dmg
            artifacts/**/*.app.tar.gz
            artifacts/**/*.deb
            artifacts/**/*.AppImage
            artifacts/**/*.rpm
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
